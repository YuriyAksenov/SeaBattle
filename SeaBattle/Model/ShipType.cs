namespace SeaBattle.Model
{
    /// <summary>
    /// Provides the enumeration of the ship type.
    /// </summary>
    public enum ShipType : byte
    {
        Empty = 0,
        One = 1,
        Two = 2,
        Three = 3,
        Four = 4
    }
}
